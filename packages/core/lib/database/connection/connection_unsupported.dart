import 'package:drift/drift.dart';

DatabaseConnection connect(String? databaseName) {
  throw UnimplementedError(
    'Plataforma no soportada. '
    'Solo se soportan plataformas nativas (móvil/desktop).',
  );
}
